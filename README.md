# HAL_JS — Visual Test Flow Editor

Lightweight visual editor to design testing workflows with draggable shapes, action blocks and orthogonal connections.

## Quick start

1. Install dependencies:
   ```sh
   npm install